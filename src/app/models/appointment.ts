
export interface Appointment {
    date?: string;
    car?: string;
    userid?: string;
    estado?: string; //si esta enviada, por confirmar o confirmada
    qrCode?: string;
    diagnosis?: string;
}